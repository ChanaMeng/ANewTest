namespace SDClub.UIFrameWork
{
    public enum UILayerType
    {
        Bottom = 0,    // 底层 (背景等)
        Center = 1,    // 中层 (主界面)
        Popup = 2,     // 弹窗层
        Top = 3,       // 顶层 (Loading、提示)
    }
    
    public enum UISizeType
    {
        FullSize = 0,
        SafeSize = 1,
    }
}
