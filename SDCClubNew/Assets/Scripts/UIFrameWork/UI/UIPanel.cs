using UnityEngine;

namespace SDClub.UIFrameWork
{
    public class UIPanel : MonoBehaviour
    {
        public UILayerType Layer = UILayerType.Center;
        public UISizeType SizeType = UISizeType.FullSize;
        public int OrderIdx;
    }
}
