using SDClub.Core;

namespace SDClub.Loader
{
    /// <summary>
    /// 游戏入口，业务逻辑启动点
    /// </summary>
    public static class Entry
    {
        public static void Start()
        {
            Log.Debug("Entry.Start - 游戏入口启动");
            // TODO: 创建主场景 Fiber，进入业务逻辑
        }
    }
}
